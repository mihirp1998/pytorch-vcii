class TempArgument():
	def __init__(self,distance1,distance2,warp,v_compress,patch,num_crops,batch_size):
		self.distance1 = distance1
		self.distance2 = distance2
		self.warp = warp
		self.v_compress = v_compress
		self.patch = patch
		self.num_crops = num_crops
		self.batch_size = batch_size
